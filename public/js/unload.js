//$(window).on('unload', function() {
	// async: false will make the AJAX synchronous in case you're using jQuery
	//$.ajax({
        //type: 'POST',
        //url: window.location + "unload",
        //async: false
   // });
//});